// const { db, TABLES } = require('../db');

// exports.createCategory = title => {
//   return db.insert({ title }).into(TABLES.JOBS).select('*');
// }

// exports.attachJobLane = (jobId, laneId) => {
//   return db.insert({ job_id: jobId, lane_id: laneId }).into(TABLES.LANES_JOBS).select('*');
// }